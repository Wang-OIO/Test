#!/usr/bin/python3
import math
import turtle

# ---输入名称规范，首字母大写---
def normalize(name):
    return name[0].upper() + name[1:].lower()
#L1 = ['adam', 'LISA', 'barT']
#L2 = list(map(normalize, L1))
#print(normalize())

def normalize(name):
    return name[0].upper() + name[1:].lower()
#测试提交

